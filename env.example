# ==============================
# Environment Configuration Template
# ==============================
# 
# IMPORTANT: This file is a template. DO NOT put real secrets here.
# Copy this file to .env.local and fill in your actual values:
#   cp env.example .env.local
#
# .env.local is gitignored and will NOT be committed to version control.
# ==============================

# ==============================
# DATABASE CONFIGURATION
# ==============================

# PostgreSQL Database URL (Prisma)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Example: postgresql://postgres:mypassword@localhost:5432/sparks_rentals
# Production: Use connection pooling URL from your provider (e.g., Supabase, Neon)
DATABASE_URL=postgresql://postgres:password@localhost:5432/mydb

# MongoDB URI (if using MongoDB for other features)
# Format: mongodb://HOST:PORT/DATABASE or mongodb+srv://USER:PASSWORD@CLUSTER/DATABASE
# Example: mongodb://localhost:27017/sparks_rentals
# Production: Use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/sparks_rentals

# ==============================
# AUTHENTICATION & SECURITY
# ==============================

# JWT Secret for authentication tokens
# CRITICAL: Use a strong, random string in production (minimum 32 characters)
# Generate with: openssl rand -base64 32
# NEVER commit the real secret to git!
JWT_SECRET=your_jwt_secret_here_change_in_production_use_openssl_rand_base64_32

# Optional: Separate secrets for access vs refresh tokens.
# If not set, the app falls back to JWT_SECRET.
JWT_ACCESS_SECRET=your_access_jwt_secret_here
JWT_REFRESH_SECRET=your_refresh_jwt_secret_here

# ==============================
# CACHING
# ==============================

# Redis Cache URL
# Format: redis://HOST:PORT or redis://USER:PASSWORD@HOST:PORT
# Example: redis://localhost:6379
# Production: Use Redis Cloud, Upstash, or AWS ElastiCache
REDIS_URL=redis://localhost:6379

# ==============================
# FILE STORAGE (AWS S3)
# ==============================
# Required for file upload features (user avatars, documents, etc.)
# Get credentials from AWS IAM Console

# AWS Region (e.g., us-east-1, eu-west-1, ap-south-1)
AWS_REGION=us-east-1

# AWS Access Key ID
# SECURITY: Never commit real credentials. Use IAM roles in production when possible.
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here

# AWS Secret Access Key
# SECURITY: Keep this secret! Rotate regularly.
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here

# S3 Bucket Name
# Create a bucket in AWS S3 console first
S3_BUCKET_NAME=sparks-rentals-uploads

# S3 Bucket Region (usually same as AWS_REGION)
S3_BUCKET_REGION=us-east-1

# ==============================
# EMAIL SERVICE (SendGrid)
# ==============================
# Required for transactional emails (password reset, notifications, etc.)
# Get API key from SendGrid Dashboard

# SendGrid API Key
# SECURITY: Keep this secret! Never expose in client-side code.
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Sender Email Address (must be verified in SendGrid)
# Example: noreply@yourdomain.com
SENDGRID_FROM_EMAIL=noreply@example.com

# Sender Name (displayed in email clients)
SENDGRID_FROM_NAME=Sparks Rentals

# ==============================
# APPLICATION ENVIRONMENT
# ==============================

# Node Environment
# Values: development | staging | production
# Affects error messages, logging, and performance optimizations
NODE_ENV=development

# Disable Next.js Telemetry
# Set to 1 to disable anonymous usage data collection
NEXT_TELEMETRY_DISABLED=1

# ==============================
# CLIENT-SIDE VARIABLES
# ==============================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# NEVER put secrets in NEXT_PUBLIC_ variables!

# Environment indicator (shown in UI)
# Values: development | staging | production
NEXT_PUBLIC_ENVIRONMENT=development

# API Base URL (for client-side API calls)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# API URL (alternative naming for consistency)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: Simulate slow network for loading skeletons (milliseconds)
# Example: 2000
NEXT_PUBLIC_FETCH_DELAY_MS=0

# ==============================
# SECURITY BEST PRACTICES
# ==============================
#
# 1. NEVER commit .env.local to git (it's in .gitignore)
# 2. Use strong, unique secrets for JWT_SECRET (minimum 32 characters)
# 3. Rotate credentials regularly (especially AWS keys and API keys)
# 4. Use environment-specific values (different secrets for dev/staging/prod)
# 5. In production, use secret management services (AWS Secrets Manager, Vercel Env Vars)
# 6. Never put secrets in NEXT_PUBLIC_ variables (they're exposed to browsers)
# 7. Use IAM roles instead of access keys when deploying to AWS
# 8. Enable MFA on all service accounts (AWS, SendGrid, etc.)
# 9. Monitor for leaked secrets using tools like git-secrets or GitHub secret scanning
# 10. Document which variables are required vs optional for each environment
#
# ==============================
# REQUIRED vs OPTIONAL
# ==============================
#
# REQUIRED (for basic app functionality):
# - DATABASE_URL
# - JWT_SECRET
# - NODE_ENV
# - NEXT_PUBLIC_API_BASE_URL
#
# OPTIONAL (for specific features):
# - MONGODB_URI (only if using MongoDB)
# - REDIS_URL (for caching, improves performance)
# - AWS_* and S3_* (for file uploads)
# - SENDGRID_* (for email notifications)
#
# ==============================
