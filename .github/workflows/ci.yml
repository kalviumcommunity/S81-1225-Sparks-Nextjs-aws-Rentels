name: CI

on:
  pull_request:
    branches: ["*"]
  push:
    branches: ["main"]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Run ESLint
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: npm run lint

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Build app
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: npm run build

  tests:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Install dependencies
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: |
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Run tests (if present)
        working-directory: s81-1225-sparks-nextjs-aws-rentels
        run: npm run test --if-present
